# 可选能力

本文简要说明 Agent 除 Tool / Skill / MCP 外的常见扩展：**RAG**、**热门问题**、**深度思考**、**自定义拦截器**。

MCP 声明式接入（`implements McpFeature`，与 `ExternalSkills` 对称）详见 [MCP.md](MCP.md) 与 [Agent开发.md §1.4](Agent开发.md#14-特性接口对比externalskills--mcpfeature)。

## 1. RAG（检索增强生成）

当 Agent 需要基于 **Milvus 知识库** 回答问题时，override **`buildDocumentRetriever()`** 返回 Spring AI `DocumentRetriever`。

基类 `buildAgent()` 会在 retriever 非空时自动挂载 `RetrievalAugmentationAdvisor`（混合检索 + 上下文注入）。

### 1.1 知识库准备

知识入库、collection 命名、分片规则由平台知识库模块管理。对话侧使用的 collection 名称须与知识库同步产物一致。

### 1.2 Retriever 实现

推荐继承平台 **`AbstractCollectionKbRetriever`**，通过 `boundCollections()` 绑定一个或多个 collection：

```java
package io.github.jerryt92.j2agent.demo;

import io.github.jerryt92.j2agent.service.rag.inf.AbstractCollectionKbRetriever;
import io.github.jerryt92.j2agent.service.rag.retrieval.Retriever;
import org.springframework.stereotype.Component;

import java.util.List;

@Component
public class DemoKbRetriever extends AbstractCollectionKbRetriever {

    public DemoKbRetriever(Retriever retriever) {
        super(retriever);
    }

    @Override
    protected List<String> boundCollections() {
        // 与 knowledge-repo 入库后的 Milvus collection 名一致；顺序决定结果优先级
        return List.of("demo_knowledge", "demo_faq");
    }
}
```

### 1.3 Agent 挂载

```java
@Component
@RequiredArgsConstructor
public class DemoAgent extends AiAgent {

    private final DemoKbRetriever demoKbRetriever;

    @Override
    protected DocumentRetriever buildDocumentRetriever() {
        return demoKbRetriever;
    }
}
```

需 import `org.springframework.ai.rag.retrieval.search.DocumentRetriever`。

### 1.4 行为说明

| 项 | 说明 |
|----|------|
| 检索时机 | 每轮用户消息经 RAG Advisor 触发（React 工具循环中会自动跳过重复检索） |
| Query 预处理 | 平台默认 Multimodal → Compression → Rewrite 链；**插件无需改动** |
| 图片输入 | 有图时 MultimodalQueryTransformer 用 VLM 生成检索 query；纯图不再 skip RAG |
| skip 条件 | 仅当无文字且无图片时 `EmptyQuerySkippingRetrievalAugmentationAdvisor` 跳过 RAG |
| 失败降级 | Milvus 检索失败时注入 fallback 文档，**不中断对话** |
| 空结果 | 无命中分片时返回空列表，模型基于对话上下文回答 |
| 多 collection | 按 `boundCollections()` 顺序逐个检索，按 `textChunkId` 去重；全部无结果且任一 collection 检索失败时注入 fallback 文档 |
| 与 LLM 配置关系 | RAG 不挂在 LLM 提供商条目上，完全由 Agent 代码决定 |
| 来源文件展示 | 默认关闭；`rag_infos` **始终落库**；override **`isRagSourceDisplayEnabled()`** 为 `true` 时才推送实时 `srcFile` 并在历史回放中展示 |

### 1.5 RAG 来源文件展示（可选）

检索命中知识库 `.md` 文件时，可在 assistant 回复底部展示可点击来源链接。需同时满足：

1. Agent 已挂载 RAG（`buildDocumentRetriever()` 非空）
2. override **`isRagSourceDisplayEnabled()`** 返回 `true`（**默认 `false`**）

```java
@Override
public boolean isRagSourceDisplayEnabled() {
    return true;
}
```

关闭时仍正常检索、注入上下文，并将 `rag_infos` 写入 `chat_context_item`；仅不向 WebSocket 推送 `srcFile`，历史接口亦不回填 `srcFile`（后续改开关即可影响新加载的历史展示）。

### 1.6 QueryTransformer 自定义（可选）

平台在 `AiAgent.buildQueryTransformers()` 中默认启用 Query 预处理链。子类 override 返回空数组可关闭：

```java
@Override
protected QueryTransformer[] buildQueryTransformers() {
    return new QueryTransformer[0];
}
```

Multimodal 步骤依赖 Agent 绑定的 **vision** 模型。详见 [Query预处理.md](../../平台/RAG机制/检索/Query预处理.md)。

## 2. 热门问题（qa-template）

空会话时在输入框上方展示快捷问题，需同时满足：

1. 放置问答模板 JSON（插件 Agent 默认 JAR 根 **`qa-template.json`**；平台内置通用助手使用 **`prompts/universal-assistant-qa-template.json`**，通过 override `getQaTemplateResourcePath()`）
2. override **`isQaTemplateEnabled()`** 返回 `true`

> **注意**：勿在 server 工程 `resources/` 根目录放置 `qa-template.json`，会与插件子智能体 ClassLoader 串读。

### 2.1 资源格式

插件 Agent：`src/main/resources/qa-template.json`：

```json
{
  "zh_CN": [
    "演示 Agent 能做什么？",
    "如何使用 echo 工具？",
    "如何查看当前时间？"
  ],
  "en_US": [
    "What can the demo agent do?",
    "How do I use the echo tool?",
    "How do I get the current time?"
  ]
}
```

### 2.2 Agent 代码

```java
@Override
public boolean isQaTemplateEnabled() {
    return true;
}
```

### 2.3 API 与前端

| 接口 | 说明 |
|------|------|
| `GET /v1/rest/j2agent/agents` | `AgentInfoDto.showHotQuestions=true` |
| `GET /v1/rest/j2agent/qa-template?agent-id=demo_agent&limit=5` | 从 Agent 定义类 ClassLoader 按 `getQaTemplateResourcePath()` 随机抽取问题 |

## 3. 深度思考（Thinking）

深度思考控制模型是否启用 extended thinking（Anthropic / Ollama 等）。

### 3.1 优先级（高 → 低）

1. 单轮 WebSocket 请求 **`ChatRequestDto.thinkingMode`**（`provider_default` / `on` / `off`）
2. Agent 默认 **`getThinkingOverride()`**
3. 管理端 LLM 配置 **`thinkingMode`**

### 3.2 Agent 默认策略

```java
import io.github.jerryt92.j2agent.service.llm.agent.inf.constant.AgentThinkingOverride;

@Override
public AgentThinkingOverride getThinkingOverride() {
    // 示例：本 Agent 默认关闭深度思考
    return AgentThinkingOverride.OFF;

    // 其它可选值：
    // USE_PROVIDER_DEFAULT — 不覆盖，沿用 LLM 配置
    // PROVIDER_DEFAULT — 强制不传 thinking 参数
    // ON — 强制开启
}
```

## 4. 自定义拦截器

`AiAgent` 默认挂载三条拦截器（工具错误兜底、工具 UI、Skill UI）。子类可 override **`buildInterceptors()`** 追加自定义 `Interceptor`：

```java
import com.alibaba.cloud.ai.graph.agent.interceptor.Interceptor;

@Override
protected Interceptor[] buildInterceptors() {
    return new Interceptor[] {
            agentUiToolEventInterceptor,
            agentUiSkillLoadToolInterceptor,
            myCustomInterceptor   // 追加
    };
}
```

**注意**：

- `AgentToolErrorReturnInterceptor` 由 `buildEffectiveInterceptors()` **强制保留**，无需（也不应试图移除）手动加入。
- 自定义拦截器应谨慎修改工具调用链路，避免与 UI 事件拦截器冲突。

## 5. 平台代码索引

| 主题 | 路径 |
|------|------|
| RAG 挂载 | `.../service/llm/agent/inf/AiAgent.java` — `buildDocumentRetriever()`、`buildQueryTransformers()` |
| Query 预处理 | `.../rag/query/DefaultQueryTransformers.java`、`MultimodalQueryTransformer.java` |
| Collection 检索基类 | `.../rag/AbstractCollectionKbRetriever.java` |
| 检索引擎 | `.../service/rag/retrieval/Retriever.java` |
| 热门问题 | `AiAgent#isQaTemplateEnabled()`、`QaTemplateController` |
| RAG 来源展示 | `AiAgent#isRagSourceDisplayEnabled()`、`TurnRagSourceRegistry`、`RagSourceFileService` |
| 深度思考枚举 | `.../service/llm/agent/AgentThinkingOverride.java` |
| MCP 声明式接入 | `.../service/llm/agent/inf/feature/McpFeature.java` — 与 `ExternalSkills` 对称，见 [MCP.md](MCP.md) |

## 6. 相关文档

- [Agent开发.md](Agent开发.md) — 可 override 方法总览
- [MCP.md](MCP.md) — `McpFeature` 声明式接入
